# New API 海外 Seedance 中转站项目日报

日期：2026-08-20

## 一、今日总结

- 完成线上 250 并发、20 秒混合压测，定位 PostgreSQL `max_connections=100` 与应用 `SQL_MAX_OPEN_CONNS=1000` 错配导致的 `SQLSTATE 53300`；将应用连接池上限调整为 80 后复测，500 错误由 101 次降为 0，异常率由 1.0% 降为 0.0%，p99 由 3389ms 降至 2380ms。
- 完成令牌页默认“聊天”子菜单清理与系统级 Playground 隐藏：`d0da6c84` 将默认 Chats 配置置空，`9d7a0b94` 让概览引导卡片跟随 Playground 可见性；上线后 `/api/status` 返回 `chats=[]`，相关入口不再显示。
- 完成渠道模型选择器与用户分组范围修复：`a23b47d9` 将候选模型收窄为已创建模型并保留自定义输入，`ad146621` 将普通用户可选分组限制为本人分组，管理员与 root 保持豁免；相关前端检查及 controller 测试通过并已上线。
- 修复 Seedance 顶层 `resolution` 被忽略的问题：`805df863` 将顶层分辨率纳入解析、校验、预扣和结算同一链路；上线后 `doubao-seedance-2.5 + 4k` 在本地返回 400，合法 1080P 请求可正常创建任务。
- 完成 `test` 组测试渠道隔离与低成本任务验证：确认测试 Token 仅路由渠道 ID `4`；真实 480P、4 秒任务完成 `提交 → IN_PROGRESS → SUCCESS → 下载`，最终结算 275,056 quota，下载文件 2,234,450 字节且 MP4 文件头有效。
- 完成生产日志脱敏抽检：应用日志与数据库 logs 表对完整渠道密钥、用户 Token 和 `Authorization` 均为 0 命中；真实 200 与 403 请求落盘后复查结果一致。
- 更新上线清单中的数据库、Redis、限流、监控、计费、视频下载与支付关闭等已验证项；正式开放前仍保留持久幂等、失败退款、视频输入、三方账单、恢复演练和异机备份等 P0 门禁。

## 二、明日计划

- 为视频创建接口补充客户端持久幂等，验证同键同请求不重复创建或扣费、同键不同请求返回冲突。
- 仅使用 `test` 组测试渠道执行真实失败退款验收，并结合自动化覆盖 401、429、500/503、超时及余额不足场景。
- 修复任务列表成功视频的预览入口，确保受保护视频通过用户鉴权加载且不暴露上游直链。
- 补做视频输入、720P 与音频开关的真实任务结算，核对预扣、差额退款、最终扣费和并发释放。
- 继续推进方舟拆分账单三方对账、成本熔断、备用渠道、恢复演练和异机备份等上线门禁。
