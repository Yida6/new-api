# New API 海外 Seedance 中转站项目日报

日期：2026-08-13

## 一、今日总结

- 完成方舟 Endpoint ID 与 API Key 防泄露 P0 修复及多轮只读复验，任务详情已移除 `properties.upstream_model_name`，任务数据、恢复记录和客户端错误出口均纳入统一脱敏。
- 完善凭据识别规则，覆盖大小写与下划线 Endpoint、`sk-` 密钥、Bearer/Authorization、带引号 JSON 键值及常见凭据字段，同时保留短值和普通业务字段，降低误脱敏风险。
- 封堵日志链路旁路，数据库落盘、文件/控制台、系统日志、标准库日志及日志 API 均统一脱敏；合法或非法 `expr_b64`、畸形历史数据和任意深度嵌套数组/对象均已覆盖。
- 补充任务、错误、日志和凭据格式的安全测试，并修复 Windows 时钟精度导致的 affinity 测试缓存键碰撞；相关主模块与 `relaykit` 测试通过，受影响包构建及静态检查通过。
- 更新开发与上线清单，关闭“不向客户端暴露方舟 Endpoint ID 和 API Key”P0；生产环境日志抽检及完整用户 Token 落盘验证继续保留为上线前事项。

## 二、明日计划

- 在生产配置下抽检数据库、文件、控制台及日志 API，确认 Endpoint ID、API Key 和完整用户 Token 均不会落盘或返回客户端。
- 验证图生视频、相同幂等键重复提交、失败与超时退款、并发上限及成本熔断等负向场景。
- 补充上游 401、429、5xx、超时和余额不足场景测试，并核对任务日志、用户扣费与火山方舟账单明细。

