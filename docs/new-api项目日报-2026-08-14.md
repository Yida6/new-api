# 项目日报

日期：2026-08-14

## 一、今日总结

### new-api 项目

- 完成非首发模型资源路由安全修复：为 `/mj/image/:id` 等入口补齐鉴权与资源所有者校验，Seedance 对外资源统一通过 `/v1/videos/:task_id/content` 代理访问，并移除响应中的上游直链及可泄露地址的错误信息。
- 完成 Seedance 数据脱敏与缓存策略收尾：任务 DTO 对 URL 字段执行 fail-closed 清洗，OpenAI Video 错误信息统一脱敏，视频资源响应改为 `Cache-Control: private, no-store`，确保每次访问均重新执行鉴权和所有权校验。
- 补充匿名访问、用户 A 越权访问用户 B 资源、所有者正常访问、代理响应头脱敏及无效数据关闭等回归用例；`go test ./relay/ ./router/ ./relay/channel/task/doubao/ -count=1` 通过，前端类型检查通过。`controller` 仍有既有的 `TestRelayTaskUpstreamTimeout` 失败，与本次安全修复无关。
- 完成受损 Git 仓库恢复与备份：建立健康工作仓库和裸仓库，恢复分支 `recovery/20260814-workspace` 固定在提交 `2ee071623a48fe22bb88994289c12ff38370227f`，工作区保持干净，裸仓库执行 `git fsck --full` 通过且备份引用与工作仓库提交一致。
- 完成旧目录清理：在确认 Git 相关文件内容已备份并校验一致后，按顺序删除旧源码目录和历史备份目录；保留 `new-api-src-recovered-20260814` 与 `new-api-fresh-20260814.git` 两个健康仓库。
- 修复仓库迁移后的 Docker Compose 容器名冲突：在 `docker-compose.dev.yml` 中固定项目名 `new-api-src`，复用原 PostgreSQL、Redis 及后端数据卷；后端容器已重新绑定恢复仓库，`/api/status` 返回 HTTP 200。

### 石材网站（Transhill 淬山生态无机石）

- 完成服务器代码版本对比与清理部署：确认 transhill-core 插件本地与服务器为同一版本（md5 一致）；本地主题较服务器旧版更新；清理服务器残留的损坏插件目录与旧主题（备份至 `~/backup_20260814/`），部署本地新版主题 + 404.php + 合并版 functions.php（28 文件 md5 全部一致），修复服务器上因 Windows 路径被当作文件名的反斜杠损坏目录结构；首页/404 正常、PHP 语法零错误。
- 修复导航栏语言跳转问题：新增 mu-plugin `pll-page-slug-fix.php` 解决 /markets/ 被 Polylang 301 至英文（parse_query 中同步修正 queried_object_id 与 queried_object）；为 en/ar/ru 分配独立语言菜单到 primary 位置；4 语言 × 8 页面全部 200。
- 修复英文站内容中文残留：恢复多语言版 footer.php/header.php；重写 template-contact.php 为多语言数组模式并应用到 en/ar/ru 联系页；4 语种联系页 H1 与页脚地址均正确。
- 完成全站多语言改造：9 个页面/归档模板改为 th_t() 多语言数组模式；transhill-core 插件 stats/faq/comparison/chips/project_grid/feature_grid 短代码按 pll_current_language 输出四语言；新增归档标题过滤器；18 个页面标题及 markets/insights 英文版正文完成翻译；28/28 页面 200。
- 根治中文站点击市场/洞察跳英文（方案A）：关闭 Polylang redirect_lang/browser 跳转；将 insight/market 由 CPT 改回普通页面（public=false/has_archive=false/rewrite=false），消除与页面 slug 的冲突；th_create_menus 升级 v3 生成语言感知 URL；本地容器验证 /insights/ /markets/ 中文页面正常。

## 二、明日计划


- 单独处理 `TestRelayTaskUpstreamTimeout` 既有失败，完善 `classifyErrorKind` 对包装错误链的遍历识别，并执行 `controller` 相关回归测试。
- 复核今日恢复提交及尚未提交的 Docker Compose 配置修改，确认变更范围后形成清晰的 Git 提交记录。
- 从恢复仓库完整执行开发环境启动流程，验证前端、后端、PostgreSQL 和 Redis 的联调状态及重启后的数据延续性。
