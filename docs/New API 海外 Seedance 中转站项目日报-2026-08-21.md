# New API 海外 Seedance 中转站项目日报

日期：2026-08-21


## 一、今日总结

- 完成 Seedance 客户端持久幂等修复并上线 `e1b5431c`：按“用户 + Token + `Idempotency-Key`”保存请求指纹与公开任务，同键同请求返回原任务，同键不同请求返回 409；生产原样重放仍返回 `task_333…gz3P`，数据库任务数保持 1，且仅预扣一次 433,851 quota。
- 完成真实失败与退款安全范围验收：三个版权限制任务均只有一笔预扣和一笔等额退款，最终 `quota=0`、`concurrency_released=true`、`token_delta_pending=0`，用户余额恢复且测试渠道保持启用；自动化回归继续覆盖 401、429、500/503、超时和余额不足。
- 完成任务列表视频预览链路修复与加载优化：`59636e8c` 恢复成功任务的结果入口，`a8c36930` 先改为通过鉴权 API 获取 Blob，`36002fd8` 进一步改为签发 5 分钟、绑定用户与任务的短时播放地址，并由浏览器通过 HTTP Range 原生分段加载，避免等待完整视频下载，同时不暴露上游结果 URL。
- 修复方舟视频编辑协议兼容：真实任务确认视频输入需要 `role=reference_video`，编辑模式要求 `ratio=adaptive`、`metadata.duration=-1`；`7356ca53` 仅对“视频输入 + adaptive”组合放行 `-1`，其他负时长仍拒绝，相关 doubao、service、relay、controller 测试及 `go build ./...` 均通过。
- 完成视频输入生产成功验收：`test` 渠道任务 `task_bIC…4p0d` 经 `NOT_START → IN_PROGRESS → SUCCESS`，方舟 usage 为 216,900 tokens；系统预扣 1,323,660、退差额 693,761、最终扣费 629,899 quota，用户余额差额一致，并发释放且无待补偿差额。
- 验证成功视频代理下载 HTTP 200、文件 4,423,508 字节且 MP4 文件头有效；新增播放链路集成测试覆盖 HTTP 206 单 Range 转发、任务绑定和多 Range 拒绝，前后端构建及相关测试通过，匿名签发与无效播放令牌均返回 401。结合既有 720P、5 秒、带音频成功任务，视频输入、720P 与音频开关均已有真实任务及系统结算证据，完整渠道密钥与测试 Token 在应用日志和数据库中仍为 0 命中；当前生产镜像 `18029155` 已继承上述修复并保持健康。
- 完成额度体系 `int64/BIGINT` 全链路改造并提交 `18029155`：用户、API Key、日志、任务、兑换、充值、债务、Redis 预扣与计费结算等额度字段统一升级为 `int64`，保留 `QuotaPerUnit=500000` 的整数记账精度，新增单 Key 业务上限 `1e15` 内部额度及前端多币种最大值提示，支持 `11111 USD`、`111111 USD` 有限额度创建与更新；同步修复 `shouldTrust` 的 int64 上下文读取回归，补齐 7 种语言翻译，相关 Go、TypeScript、lint 与前端额度测试通过。
- 完成 PostgreSQL 16 真实迁移测试，修正 PG/GORM 将 `BIGINT` 报告为 `int8` 导致的测试误判，确认旧 `2147483647` 数据完整保留且 `55,555,500,000` 可正常写入读取；发布前生成备份 `/opt/new-api/backups/newapi-20260821T042757Z.sql.gz`，生产升级至镜像 `new-api-central-asia:18029155`，容器健康、首页与 `/channels` 返回 HTTP 200，`/api/status` 返回 `quota_per_unit=500000`、`max_quota=1000000000000000`，相关生产额度列均为 `BIGINT`。
- 更新生产异常验收报告和 3.1 清单，勾选失败安全范围验收及视频输入/分辨率/音频实际结算；方舟拆分账单接口仍返回“当前账号未开通拆分账单能力”，且当日账单为 T+1，因此最终财务三方对账保持未完成。

## 二、明日计划

- 协调开通方舟拆分账单能力，并在 T+1 数据可用后核对方舟 CNY 明细、系统任务日志和用户扣费，完成三方财务对账。
- 验证成本熔断与备用渠道切换，确认达到阈值或主渠道不可用时不会产生失控费用或重复任务。
- 使用最新生产备份在隔离环境执行恢复演练，并补充异机备份落地与恢复记录。
- 使用生产页面创建并回读一个 `111111 USD` 有限额度 API Key，执行小额调用并核对钱包余额、Key 剩余额度与消费日志；持续观察镜像 `18029155` 的容器健康、视频分段预览、任务轮询、退款结算和 HTTP 4xx/5xx，完成 int64 改造最终业务验收并确认无新增异常。
- 推进 Passkey/2FA、灰度开放日期与 CDN 转存等剩余上线门禁，完成后同步更新正式开放清单。
